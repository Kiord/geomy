[package]
name = "geomy"
version = "0.1.0"
description = "A 3D mesh toolbox"
authors = ["Geomy contributors"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
